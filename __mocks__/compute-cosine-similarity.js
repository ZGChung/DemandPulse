/* eslint-disable no-unused-vars */
// Mock for compute-cosine-similarity module
module.exports = function cosineSimilarity(_vecA, _vecB) {
  // Simple mock returning random similarity between 0 and 1
  // Parameters are intentionally unused in mock
  return Math.random();
};
